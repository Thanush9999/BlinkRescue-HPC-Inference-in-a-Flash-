# BlinkRescue-HPC-Inference-in-a-Flash-
AI models (CNNs/Transformers) can detect damaged buildings, blocked roads, or stranded  people, but inference over thousands of images/videos in minutes is computationally heavy.  HPC clusters can speed this up — but raw AI inference is costly. Hence, optimization  (quantization, pruning, batching) is needed.
